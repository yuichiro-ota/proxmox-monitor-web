// ---------------------------------------------------------------------------
// 系統図（トポロジ）の静的設定
// ---------------------------------------------------------------------------
// ルーター / ネットワークセグメント / クラスタ名など、Proxmox API からは
// 取得できない「構成情報」をここで定義します。環境に合わせて編集してください。
//
// 将来オンプレサーバーやネットワーク機器（node_exporter など）を追加する際は
// `extraGroups` にグループを足すと、クラスタと並んで系統図に表示されます。
// ---------------------------------------------------------------------------

export const topology = {
  // 最上位のルーター（ゲートウェイ）
  // 現在は SoftBank Air。API での問い合わせ（死活監視）には対応していないため
  // 静的に ONLINE 表示しています。
  // ※ 以前の YAMAHA(RTX) では SNMP 等で実際に死活監視していたので、
  //   将来 YAMAHA に戻す/追加する際は router 監視を実装して online を動的化する。
  router: {
    name: 'SoftBank Air',
    ip: '192.168.3.1',
    online: true,
  },

  // ルーター配下のネットワークセグメント。複数定義可。
  // SoftBank Air は VLAN 非対応なので vlan は空にしています。
  segments: [
    {
      cidr: '192.168.3.0/24',
      vlan: '',
      label: 'LAN',
    },
  ],

  // Proxmox クラスタの見出し。ノード自体は API から動的に取得します。
  cluster: {
    name: 'Proxmox ss-cluster',
    // クラスタをぶら下げるセグメント（segments の cidr）。null なら先頭に接続。
    segment: '192.168.3.0/24',
  },

  // 将来追加するグループ（例: オンプレサーバー, ネットワーク機器）。
  // 空配列の間は系統図に表示されません。
  //   { name: 'オンプレサーバー', segment: '192.168.100.0/24', items: [...] }
  extraGroups: [],
}
